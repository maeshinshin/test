# Public base URL of the ConnectRPC backend.
# In dev, leave empty to use the same origin (works with the Vite proxy).
# In production on a public IP / domain, set to e.g. "https://api.example.com".
VITE_API_BASE_URL=
